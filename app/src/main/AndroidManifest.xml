<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anand.buspasssystem">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash12">
            </activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" >

        </activity>
        <activity android:name=".Addbus" >

        </activity>
        <activity android:name=".ChoseBus" />
        <activity android:name=".seatbooking" />
        <activity android:name=".ticketgenerate" />
        <activity android:name=".conductor" />
        <activity android:name=".payment">

        </activity>
        <activity android:name=".Availablebus" >
            </activity>
        <activity android:name=".thankyou" />
    </application>

</manifest>